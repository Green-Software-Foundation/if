name: integration-template
description:
tags:
initialize:
  plugins:
     'sci-e':
        model: ${model}
        path: ${path}
tree:
  children:
    child:
      pipeline:
        - ${name}
      config:
      inputs:
        - ${input}
