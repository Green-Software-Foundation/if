name: ccf-demo
description: example impl invoking CCF model
initialize:
  models:
    - name: azure-importer
      kind: builtin
graph:
  children:
    child:
      pipeline:
        - azure-importer
      config:
        azure-importer:
      observations:
          timestamp: '2023-11-01T14:45:00Z'
          metric: 'Percentage CPU'
          interval: 'PT1M'
          timespan: 'PT1H'
          aggregation: 'avg'

